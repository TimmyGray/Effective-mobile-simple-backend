# Implement Backend Feature

## Outcome

Deliver production-ready backend functionality for the requested task, including endpoint behavior,
schema/migration updates when needed, and policy-aligned access checks.

## Context

This project targets a custom auth/authz backend. Follow `ARCHITECTURE.md` module boundaries
and `docs/CONVENTIONS.md` requirements for serializer validation, service-layer logic, and
soft-delete user semantics.

## Scope

In scope: backend API implementation, data model updates, auth/authz enforcement, and related tests.
Out of scope: unrelated roadmap refactors and frontend-only work.
